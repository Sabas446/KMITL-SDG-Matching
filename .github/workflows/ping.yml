name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "*/10 * * * *"  # ทุก 10 นาที
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Streamlit App
        run: curl -s https://kmitl-sdg-matching.streamlit.app
